---
Title: Stats
Description: Aggregate blog traffic — HTML pages and JSON API endpoints — with no third-party trackers. Counters live in local Redis.
Template: stats
Lang: en
Translation_Key: praderas-nav-stats
---

**Approximate** traffic: browser page views and JSON API consumption (`/blog.json`, `/search.json`, etc.). We do not store IPs or user profiles — only aggregate counters.

Machine-readable snapshot: [`/stats.json`](/stats.json).
